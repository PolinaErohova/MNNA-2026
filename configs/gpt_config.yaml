model:
  vocab_size: 50257
  d_model: 512
  n_heads: 8
  n_layers: 8
  d_ff: 2048
  max_seq_len: 512
  dropout: 0.15

training:
  batch_size: 16
  learning_rate: 2e-4
  weight_decay: 0.05
  max_epochs: 30
  warmup_steps: 1000
  max_grad_norm: 1.0
  checkpoint_dir: checkpoints/
  log_dir: logs/
